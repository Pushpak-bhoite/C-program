//An isogram is a word or phrase in which no letter occurs more than once
//Ex:dialogue,isogram
//deed,intestine
#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void isogram(char * s){
    int l=strlen(s);
    for (int i = 0; s[i] != 0; i++)
    {
        for (int j = i+1; j = 0; j++)
        {
            
        }
        
    }
    
}
int main(){
    char s[50]="deed";
    isogram(s);
}